# Training dependencies — install in a separate environment from requirements.txt
# These are heavy GPU packages not needed to run the app itself.
#
# Recommended setup:
#   python -m venv .venv-train
#   source .venv-train/bin/activate
#   pip install -r requirements-train.txt
#
# Hardware: a single NVIDIA GPU with >=16 GB VRAM (24 GB recommended for 8B).
# For CPU-only debugging, remove bitsandbytes and set load_in_4bit=False.

# Core training stack
trl>=0.9.0
peft>=0.10.0
transformers>=4.44.0
accelerate>=0.30.0
datasets>=2.18.0

# 4-bit quantization (Linux/CUDA only; skip on macOS)
bitsandbytes>=0.43.0

# Experiment tracking
mlflow>=2.12.0

# Flash Attention 2 for faster training (optional; requires CUDA 11.6+)
# flash-attn>=2.5.0  # uncomment if your GPU supports it

# Already in requirements.txt but listed here for completeness in a standalone env
openai>=1.30.0
