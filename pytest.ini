[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    llm: marks tests that make real LLM calls — deselect with '-m "not llm"'
addopts =
    -v
    --tb=short
    --cov=app
    --cov-report=term-missing
    --cov-report=html:htmlcov
